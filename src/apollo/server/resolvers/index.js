import { productsResolver } from "./products.resolvers";
import { brandResolver } from "./Brand.resolvers";
import { categoryResolver } from "./category.resolvers";
import { colorResolver } from "./color.resolvers";
import { addToCartResolver } from "./addToCart.resolvers";
import { wishlistResolver } from "./wishlist.resolvers";
import { userResolver } from "./user.resolvers";
import { roleResolver } from "./role.resolvers";

export {
  productsResolver,
  brandResolver,
  categoryResolver,
  colorResolver,
  addToCartResolver,
  wishlistResolver,
  userResolver,
  roleResolver,
};
